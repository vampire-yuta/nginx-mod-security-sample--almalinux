Vagrant.configure("2") do |config|
  config.vm.box = "almalinux/9"

  config.vm.hostname = "almalinux9-nginx-mod-security"

  config.vm.provider "virtualbox" do |vb|
    vb.name = "almalinux9-nginx-mod-security"
    vb.memory = "4096"
    vb.cpus = 2
  end
end
